---
milestone: v1.0
audited: 2026-05-28
status: passed
scores:
  requirements: 6/6 phases delivered + verified
  phases: 6/6 verify-work PASS
  integration: 8/8 seams WIRED
  flows: 1/1 E2E referral flow complete
gaps: {}   # zero critical blockers
tech_debt:
  - phase: cross-phase
    items:
      - "W1: conversions WebhookBodySchema.customer_email is z.string().email() (strict). A malformed/empty email from financeiro → 400 validation_error, and the sender retries only on 5xx, so such a sale silently fails to book. Fix when wiring real financeiro data: assert valid email at the financeiro checkout boundary, or relax/normalize the schema (email is only used for the salted hash + leads PII)."
      - "W2: Phase 06 patch sends external_order_id = orgId (spec § 6 illustrates subscription_id). Internally consistent (idempotency derived from same value both sides; markOrgFirstPaid is once-per-org). Fine for the v1.0 one-org-one-sale model; revisit if multi-sale-per-org (re-subscribe after churn) is introduced — second sale would collide on idempotency_key."
      - "audit_log: pre-Phase-05 rows (Phases 02/03 admin writes) used '' hash placeholders and are OUTSIDE the hash chain; new rows chain from genesis. Backfill not required for v1.0."
  - phase: spec-deferred (out of scope by design, not gaps)
    items:
      - "Mobile app (apps/mobile scaffold untouched)"
      - "Asaas auto-PIX payouts (v1.0 is manual + CSV export)"
      - "Finder-specific commission rate overrides (per-product flat in v1.0)"
      - "gps-comercial / aluga-flow / other sibling integrations (separate milestones)"
      - "Two-person approval UI for high-value commission adjustments (schema supports it; UI deferred)"
      - "DSAR self-service UI (admin handles manually)"
      - "Cloudflare Turnstile on /r/:code (Upstash rate-limit only in v1.0)"
      - "refund-webhook idempotency hardening (v1.1)"
      - "/signup rate-limit (v1.1; honeypot only in v1.0)"
operational_prerequisites:   # not code gaps — environment/ops before production
  - "Clerk session-token custom claim { \"publicMetadata\": \"{{user.public_metadata}}\" } must be set in the Clerk dashboard or backend userRole is undefined → admin gates 403 (doc: docs/nexo/decisions/phase01-clerk-config.md)"
  - "Clerk Restricted mode + disable end-user org creation (invite-only finders)"
  - "Real allowed_redirect_hosts + a freshly-rotated webhook_signing_secret for the fxl-financiero app row (seed ships a generated placeholder)"
  - "UPSTASH_REDIS_REST_URL + TOKEN for /r/:code rate limiting (limiter degrades gracefully without them)"
  - "FXL_FINDERS_API_URL + FXL_FINDERS_WEBHOOK_SECRET in fxl-financiero env (cross-repo patch)"
  - "Production sa-east-1 residency (LGPD)"
cross_repo:
  repo: /Users/cauetpinciara/Documents/fxl/projetos/clientes/1-fxl-financeiro
  branch: feat/fxl-finders-integration   # LOCAL ONLY — not pushed
  patch: docs/nexo/cross-repo/06-financeiro-integration.patch
  status: awaiting human review (do not merge until reviewed)
---

# FXL Finders v1.0 — Milestone Audit

**Verdict: ✅ PASSED.** All 6 phases delivered, verify-work PASS on each, code-review clean (Criticals found+fixed inline during execution), whole-monorepo type-check green, and a post-execution cross-phase integration check confirmed all 8 seams WIRED + the full spec §6 referral→checkout→commission→payout flow completing end-to-end in the built code.

## Phase results

| Phase | Scope | Tasks | verify-work | code-review | commit |
|---|---|---|---|---|---|
| 01 | Schema + Clerk auth + RLS | 17/17 | PASS 19/19 (01-UAT.md) | 0 Crit / 0 Warn | 46c05dc |
| 02 | Apps/products/price-bands admin | 9/9 | PASS (02-UAT.md) | 1 Crit fixed (secret leak) | 26063ea |
| 03 | Finder onboarding + portals | 14/14 | PASS 26/26 (03-UAT.md) | 0 Crit (2 caught in TDD) | 68912b4 |
| 04 | Referral links + redirect + clicks | 10/10 | PASS 30/30 (04-UAT.md) | 0 Crit / 2 Warn fixed | e110c07 |
| 05 | Conversion + commission + audit | 14/14 | PASS 28/28 (05-UAT.md) | 1 Crit fixed (refund mis-book) | 16d1a6f |
| 06 | financiero integration + payout CSV | 12/12 | PASS (06-VERIFY.md) | 1 fixed (test DB race) | 2ac9b66 |

Test totals at close: ~149 API (unit+integration) + web i18n + site (ua-family + click-handler) + shared-utils HMAC — all green. RLS proven fail-closed as the non-superuser `fxl_finders_app` role.

## Cross-phase integration (post-execution, on built code)

8/8 seams WIRED: HMAC byte-match (shared-utils ↔ Phase 05 middleware ↔ Phase 06 inline), webhook body contract (15 fields identical), idempotency_key (sha256 identical both sides), canonical slug `fxl-financiero`, commission lifecycle reaching terminal `paid`, single `payouts` table, journaled RLS + `referral_links` public-read, UUID-based `resolveFinderId`. Full E2E flow (link → /r redirect → financiero checkout persist → first_paid webhook → HMAC-verified idempotent ingest → nightly promote → payout batch + CSV → mark-paid) completes without a break.

## Process note

This milestone ran a two-stage adversarial review BEFORE execution (Phase 2.5): a 7-agent plan review found 22 BLOCKERs (incl. a slug typo that would 401 every webhook, a dead commission lifecycle, and a payout domain designed twice), 18 cross-cutting reconciliations were locked (D-A..D-R in plan-brief.md), a 6-agent patch pass applied 107 edits, and a re-audit confirmed 22/22 resolved + fixed 1 newly-introduced blocker. That up-front investment is why all 6 phases executed without a cross-phase integration failure.

## Next

All requirements met, zero critical blockers → eligible for `/nexo:close-milestone` (or `/gsd:complete-milestone v1.0`). Cross-repo financiero patch awaits human review before merge. Operational prerequisites above must be satisfied before production.
